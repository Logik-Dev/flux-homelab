apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: traefik
spec:
  imageRepositoryRef:
    name: traefik
  filterTags:
    pattern: ^v(?P<semver>[0-9]*\.[0-9]*\.[0-9]*)
    extract: '$semver'
  policy:
    semver: 
      range: v3.x
