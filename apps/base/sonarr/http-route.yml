---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: sonarr
spec:
  parentRefs:
    - name: traefik
      namespace: infrastructure 
      sectionName: internal-https
  rules:
    - backendRefs:
        - name: sonarr
          port: 80
